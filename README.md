This repo contains files for exploiting the [voltage glitching vulnerability](https://github.com/stacksmashing/airtag-glitcher) in apple airtags to dump and re-flash firmware.

The `glitch.py` file is intended to be run on a raspberry pi to start the process.
